import React from "react";
import styles from "./style.module.css";

export const HoverPuzzle = () => {
  return (
    <button className={`${styles.button}`}>
      <span>Puzzle</span>
    </button>
  );
};
