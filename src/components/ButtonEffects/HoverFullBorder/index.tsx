import React from "react";
import styles from "./style.module.css";

export const HoverFullBorder = () => {
  return (
    <button className={`${styles.button}`}>
      <span>Full Border</span>
    </button>
  );
};
